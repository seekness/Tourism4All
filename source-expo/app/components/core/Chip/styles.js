import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 32,
    borderRadius: 16,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    borderWidth: 1,
  },
});
